name: deploy
on:
  push:
    branches: [main]
jobs:
  deploy:
    runs-on: self-hosted
    steps:
      - name: checkout code
        uses: actions/checkout@v4

      - name: Setup Node 20
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install and Build
        run: |
          npm ci
          npm run build

      - name: Copy dist to /_work/portfolio folder and Deploy
        run: |
          mkdir -p /_work/portfolio/dist

          echo "Copying dist to /_work/portfolio"
          sudo rm -rf /_work/portfolio/dist
          sudo cp -r ./dist /_work/portfolio/dist

          echo "Copying docker files"
          sudo cp docker-compose.yml nginx.conf /_work/portfolio/

          echo "Restarting Containers"
          cd /_work/portfolio
          sudo docker-compose up -d --build --no-deps portfolio
          sudo docker-compose ps
        

